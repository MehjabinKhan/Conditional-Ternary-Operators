// Conditional Ternary Operators
let isStuding = true;
let maths = isStuding ? "Linear Equation" : "Quadratic Equation";
console.log(`You should be fresh mind for: ${maths}.`);

let isGoing = false;
let shopping = isGoing ? "Clothes" : "Makeup";
console.log(`I am going to purchase some: ${shopping}.`);

let areBaking = true;
let cakes = areBaking ? "Chocolate" : "Vanilla";
console.log(`We are baking: ${cakes}`);

let isHungry = false;
let snack = isHungry ? "Chips" : "Juice";
console.log(`you should take some: ${snack}`);